import React from 'react';

// This component is currently not used in the main application flow,
// but is being provided as a valid component to replace placeholder content.
const ApiKeyModal: React.FC = () => {
  return null; // Render nothing
};

export default ApiKeyModal;