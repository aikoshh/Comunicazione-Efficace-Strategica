// services/firebaseService.ts
// Firebase è stato scollegato. I servizi sono stati mockati e usano localStorage.

// Esportiamo null per mantenere la compatibilità degli import, ma questi
// valori non vengono più utilizzati dai servizi mockati.
export const app = null;
export const auth = null;
export const db = null;
